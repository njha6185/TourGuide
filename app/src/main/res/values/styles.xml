<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="textViewSpacingStyle">
        <item name="android:layout_marginBottom">@dimen/_5dp</item>
        <item name="android:padding">@dimen/_5dp</item>
        <item name="android:textSize">@dimen/_15sp</item>
        <item name="android:text">Name List</item>
    </style>
</resources>
